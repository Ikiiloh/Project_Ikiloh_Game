{
  "name": "ikiiloh-game",
  "version": "1.0.0",
  "description": "Game deals finder application",
  "scripts": {
    "build": "node build.js",
    "predeploy": "npm run build"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
